getCard:
  handler: src/handlers/get-card.lambda_handler
  description: Get card
  timeout: 15
  events:
    - http:
        path: /card
        integration: lambda
        method: get
        cors: true